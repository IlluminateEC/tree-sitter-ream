[package]
authors = [ "Khaumi <khaumu@khaumi.dev>" ]
autoexamples = false
categories = [ "parser-implementations", "parsing", "text-editors" ]
description = "Ream langauge"
edition = "2024"
keywords = [ "incremental", "parsing", "tree-sitter", "ream" ]
license = "MIT"
name = "tree-sitter-ream"
readme = "README.md"
repository = "https://github.com/tree-sitter/tree-sitter-ream"
version = "0.1.0"

build = "bindings/rust/build.rs"
include = [ "bindings/rust/*", "grammar.js", "queries/*", "src/*", "tree-sitter.json", "/LICENSE" ]

[lib]
path = "bindings/rust/lib.rs"

[dependencies]
tree-sitter-language = "0.1"

[build-dependencies]
cc = "1.2"

[dev-dependencies]
tree-sitter = "0.27.0"
